function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

import React from 'react';
var Slide = require('./slide');

var Slideshow = function (_React$PureComponent) {
  _inherits(Slideshow, _React$PureComponent);

  function Slideshow() {
    _classCallCheck(this, Slideshow);

    return _possibleConstructorReturn(this, _React$PureComponent.apply(this, arguments));
  }

  Slideshow.prototype.getChildren = function getChildren(children) {
    var _this2 = this;

    var processedChildren = [];
    React.Children.forEach(children, function (child) {
      if (typeof child === 'string') {
        return;
      }
      if (child.type.name && child.type.name.toLowerCase() === 'slide' || child.type.prototype instanceof Slide) {
        processedChildren.push(child);
      } else {
        processedChildren = processedChildren.concat(_this2.getChildren(child.props.children));
      }
    });
    return processedChildren;
  };

  Slideshow.prototype.render = function render() {
    return React.createElement(
      'div',
      { className: 'slideshow', style: { position: 'relative' } },
      this.getChildren(this.props.children)[this.props.currentSlide - 1]
    );
  };

  return Slideshow;
}(React.PureComponent);

Slideshow.defaultProps = {
  currentSlide: 1
};

export default Slideshow;