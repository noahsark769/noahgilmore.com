Talk about my opinions on how to format Swiftui code:

1. If the modifier is modifying a trailing closure, next line aligned with brace
2. If the modifier is modifying a not trailing closure, next line indented
3. Either all modifiers on one line or all modifiers on different lines
